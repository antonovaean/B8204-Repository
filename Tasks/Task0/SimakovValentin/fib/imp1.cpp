#include "mylib.h"
#include <stdio.h>
#include <math.h>


void sayhello() {
    printf("Hello Test\n");
}
int aplusb(int a, int b) {
    return b + a;
}
int fib(int n, int& c) {
    //return (int)((pow(((1 + sqrt(5)) / 2), n) - pow(((1 - sqrt(5)) / 2), n))/sqrt(5)); 
}