#include <stdio.h>
#include <iostream>
#include <ctime>
/*
class matrix{
    private:
        int rows, cols;
        int** table;
    public:
        matrix(int n, int m);
        void fill(int a, int b);
        void print();
        friend matrix operator + (matrix m1, matrix m2);
        friend matrix operator * (matrix m1, matrix m2);
};
*/
